# دليل إعداد المشروع - Project Setup Guide

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:
- [Node.js](https://nodejs.org/) (الإصدار 18 أو أحدث)
- [Git](https://git-scm.com/)

---

## 🚀 الخطوة 1: إنشاء حساب Supabase

1. اذهب إلى [supabase.com](https://supabase.com)
2. اضغط على **Start your project**
3. سجل دخول باستخدام GitHub أو البريد الإلكتروني
4. اضغط على **New Project**
5. اختر اسم للمشروع وكلمة مرور لقاعدة البيانات
6. اختر المنطقة الأقرب لك
7. اضغط **Create new project**
8. انتظر حتى يتم إنشاء المشروع (دقيقة تقريباً)

---

## 🗄️ الخطوة 2: إنشاء قاعدة البيانات

1. في لوحة تحكم Supabase، اذهب إلى **SQL Editor** من القائمة الجانبية
2. افتح ملف `database-backup/full-backup.sql` من المشروع
3. انسخ كل محتوى الملف
4. الصقه في SQL Editor
5. اضغط **Run** أو **Ctrl+Enter**
6. انتظر حتى تظهر رسالة نجاح

---

## 🔑 الخطوة 3: الحصول على مفاتيح الاتصال

1. في لوحة تحكم Supabase، اذهب إلى **Project Settings** (أيقونة الترس)
2. اختر **API** من القائمة الجانبية
3. ستجد المعلومات التالية:

| المعلومة | المكان |
|----------|--------|
| **Project URL** | في قسم Project URL |
| **anon public key** | في قسم Project API keys |
| **Project ID** | الجزء الأول من Project URL (بين `https://` و `.supabase.co`) |

**مثال:**
- إذا كان Project URL هو: `https://abcd1234xyz.supabase.co`
- فإن Project ID هو: `abcd1234xyz`

---

## ⚙️ الخطوة 4: تعديل ملف البيئة

1. افتح ملف `.env` في المشروع
2. استبدل القيم الموجودة بالقيم الخاصة بك:

```env
VITE_SUPABASE_PROJECT_ID="ضع-project-id-هنا"
VITE_SUPABASE_PUBLISHABLE_KEY="ضع-anon-key-هنا"
VITE_SUPABASE_URL="https://ضع-project-id-هنا.supabase.co"
```

**مثال حقيقي:**
```env
VITE_SUPABASE_PROJECT_ID="abcd1234xyz"
VITE_SUPABASE_PUBLISHABLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
VITE_SUPABASE_URL="https://abcd1234xyz.supabase.co"
```

---

## 📦 الخطوة 5: تثبيت Supabase CLI

### على Windows:
1. افتح PowerShell كمسؤول
2. اكتب الأمر التالي:
```powershell
npm install -g supabase
```

### على Mac:
```bash
brew install supabase/tap/supabase
```

### على Linux:
```bash
npm install -g supabase
```

---

## 🔐 الخطوة 6: تسجيل الدخول وربط المشروع

1. افتح Terminal أو Command Prompt في مجلد المشروع
2. سجل دخول إلى Supabase:
```bash
supabase login
```
سيفتح المتصفح لتسجيل الدخول

3. اربط المشروع:
```bash
supabase link --project-ref ضع-project-id-هنا
```

**مثال:**
```bash
supabase link --project-ref abcd1234xyz
```

سيطلب منك كلمة مرور قاعدة البيانات التي أنشأتها في الخطوة 1

---

## ☁️ الخطوة 7: نشر Edge Functions

انشر كل دالة بالأوامر التالية:

```bash
supabase functions deploy parse-pdf
supabase functions deploy generate-questions
supabase functions deploy generate-diagram
```

---

## 🔒 الخطوة 8: إضافة المفاتيح السرية

أضف مفتاح Lovable API:

```bash
supabase secrets set LOVABLE_API_KEY="ضع-المفتاح-هنا"
```

---

## ▶️ الخطوة 9: تشغيل المشروع

1. تثبيت المكتبات:
```bash
npm install
```

2. تشغيل المشروع:
```bash
npm run dev
```

3. افتح المتصفح على: `http://localhost:5173`

---

## 🌐 النشر على الإنترنت

### باستخدام Vercel:
1. ارفع المشروع على GitHub
2. اذهب إلى [vercel.com](https://vercel.com)
3. اربط حساب GitHub
4. اختر المشروع
5. أضف متغيرات البيئة في إعدادات Vercel:
   - `VITE_SUPABASE_PROJECT_ID`
   - `VITE_SUPABASE_PUBLISHABLE_KEY`
   - `VITE_SUPABASE_URL`
6. اضغط Deploy

### باستخدام Netlify:
نفس الخطوات مع [netlify.com](https://netlify.com)

---

## ❓ حل المشاكل الشائعة

### مشكلة: "relation does not exist"
**الحل:** تأكد من تنفيذ ملف `full-backup.sql` بالكامل

### مشكلة: "Invalid API key"
**الحل:** تأكد من نسخ `anon public key` بالكامل بدون مسافات

### مشكلة: "Edge Function not found"
**الحل:** تأكد من نشر جميع Edge Functions بالخطوة 7

### مشكلة: "CORS error"
**الحل:** تأكد من أن Edge Functions تم نشرها بنجاح

---

## 📞 للمساعدة

إذا واجهت أي مشكلة:
1. تأكد من اتباع الخطوات بالترتيب
2. تأكد من صحة جميع المفاتيح والمعرفات
3. جرب إعادة تشغيل المشروع

---

## 📁 هيكل الملفات المهمة

```
المشروع/
├── .env                          # متغيرات البيئة (تحتاج تعديل)
├── database-backup/
│   └── full-backup.sql          # نسخة قاعدة البيانات
├── supabase/
│   ├── config.toml              # إعدادات Supabase
│   └── functions/               # Edge Functions
│       ├── parse-pdf/
│       ├── generate-questions/
│       └── generate-diagram/
└── src/
    └── integrations/
        └── supabase/
            └── client.ts        # ملف الاتصال (لا تعدله)
```
