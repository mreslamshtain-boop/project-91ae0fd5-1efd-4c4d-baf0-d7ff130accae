# نسخة احتياطية كاملة لقاعدة البيانات
# Complete Database Backup

تاريخ الإنشاء: 2026-01-07

---

## 📁 محتويات هذا المجلد

| الملف | الوصف |
|-------|-------|
| `schema.sql` | هيكل قاعدة البيانات (الجداول والأعمدة) |
| `enums.sql` | أنواع البيانات المخصصة (Enums) |
| `functions.sql` | جميع الدوال (Functions) |
| `policies.sql` | سياسات أمان صف البيانات (RLS Policies) |
| `indexes.sql` | الفهارس (Indexes) |
| `storage.sql` | إعدادات التخزين (Storage Buckets & Policies) |
| `full-backup.sql` | **النسخة الكاملة في ملف واحد** ✨ |

---

## 🚀 كيفية استعادة قاعدة البيانات

### الطريقة السريعة (موصى بها):

1. **إنشاء مشروع Supabase جديد**
   - اذهب إلى [supabase.com](https://supabase.com)
   - أنشئ مشروع جديد

2. **فتح SQL Editor**
   - في لوحة تحكم Supabase
   - اضغط على **SQL Editor** من القائمة الجانبية

3. **تنفيذ النسخة الاحتياطية**
   - افتح ملف `full-backup.sql`
   - انسخ كل المحتوى
   - الصقه في SQL Editor
   - اضغط **Run** أو **Ctrl+Enter**

4. **التحقق من النجاح**
   - اذهب إلى **Table Editor**
   - تأكد من وجود الجداول: `exams`, `questions`, `users`, `user_roles`

---

### الطريقة المفصلة (للتحكم أكثر):

قم بتشغيل الملفات بالترتيب التالي في SQL Editor:

```
1. enums.sql      ← أولاً: أنواع البيانات
2. schema.sql     ← ثانياً: الجداول
3. indexes.sql    ← ثالثاً: الفهارس
4. functions.sql  ← رابعاً: الدوال
5. policies.sql   ← خامساً: السياسات
6. storage.sql    ← أخيراً: التخزين
```

---

## 📊 الجداول المنشأة

| الجدول | الوصف |
|--------|-------|
| `exams` | الاختبارات |
| `questions` | الأسئلة |
| `users` | المستخدمين |
| `user_roles` | أدوار المستخدمين |

---

## 🔐 الدوال المنشأة

| الدالة | الوصف |
|--------|-------|
| `create_user_with_password` | إنشاء مستخدم جديد بكلمة مرور مشفرة |
| `verify_user_login` | التحقق من بيانات تسجيل الدخول |
| `update_user_password` | تحديث كلمة مرور المستخدم |
| `has_role` | التحقق من دور المستخدم |
| `update_updated_at_column` | تحديث تاريخ التعديل تلقائياً |

---

## 📦 Storage Buckets

| Bucket | الوصف | عام؟ |
|--------|-------|------|
| `question-images` | صور الأسئلة | ✅ نعم |

---

## ⚠️ ملاحظات مهمة

1. **لا تنسَ تفعيل RLS**: سياسات الأمان مفعلة على جميع الجداول
2. **كلمات المرور مشفرة**: تستخدم bcrypt للتشفير
3. **تحتاج Edge Functions**: بعد استعادة قاعدة البيانات، انشر Edge Functions

---

## 🔗 الخطوة التالية

بعد استعادة قاعدة البيانات، راجع ملف `SETUP.md` في المجلد الرئيسي للمشروع لإكمال الإعداد.
